version: 2

models:
  - name: stg_orders
    description: one unique order per row
    columns:
      - name: order_id
        description: unique and not null
      - name: ship_mode
        description: '{{doc("ship_mode")}}'